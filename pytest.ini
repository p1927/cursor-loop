[pytest]
testpaths = tests/segments tests/integration
pythonpath = tests
markers =
    segment: small isolated unit tests (single module/function)
    integration: multiple components wired together in isolated temp project
